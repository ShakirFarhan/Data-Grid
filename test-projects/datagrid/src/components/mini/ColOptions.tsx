import React, { useMemo } from 'react';

function ColOptions() {
  return (
    <div
      style={{
        position: 'absolute',
        top: '4px',
        left: '-60px',
      }}
    >
      ColOptions
    </div>
  );
}

export default ColOptions;
